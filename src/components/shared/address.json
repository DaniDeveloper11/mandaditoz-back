{
  "collectionName": "components_shared_addresses",
  "info": {
    "displayName": "Address",
    "icon": "map-marker",
    "description": "Dirección física dentro de una ciudad. Estado/ciudad/colonia se manejan como relaciones en business."
  },
  "options": {},
  "attributes": {
    "street": {
      "type": "string",
      "required": true,
      "maxLength": 200
    },
    "exteriorNumber": {
      "type": "string",
      "maxLength": 20
    },
    "interiorNumber": {
      "type": "string",
      "maxLength": 20
    },
    "postalCode": {
      "type": "string",
      "regex": "^[0-9]{5}$"
    },
    "references": {
      "type": "text",
      "maxLength": 300,
      "description": "Referencias para ubicar (ej. 'entre calle X y Y')"
    },
    "rawText": {
      "type": "string",
      "maxLength": 500,
      "description": "Fallback: dirección completa como texto libre (útil para importación desde seed)"
    }
  }
}
